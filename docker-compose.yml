services:
  ragent:
    build: .
    volumes:
      - .:/app
      - ${WORKSPACE_PATH:-./workspace}:/workspace
    environment:
      - RAGENT_WORKSPACE=/workspace
      - OPENAI_API_KEY
      - OPENAI_BASE_URL
      - RAGENT_MODEL
